apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "hello.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
